/* en.ts — English dictionary.
   This object is the SOURCE OF TRUTH for the `Dict` type: every other locale
   must match its shape exactly, or TypeScript complains (see hi.ts). */

export type V = string | number;

export const en = {
  code: 'en',
  label: 'English',
  short: 'EN',
  htmlLang: 'en',

  site: {
    brand: 'Algo Adda',
    title: 'Algo Adda — Sorting & Searching, step by step',
    desc: 'Understand sorting and searching step by step — with Hootie the owl.',
    kicker: 'One step. One reason. Every time.',
    heroA: 'Watch an algorithm',
    heroEm: 'actually run',
    heroSub: 'Type in your own array, press Next, and Hootie explains the reason behind every single move — the way a senior walks a junior through it.',
    sorting: 'Sorting',
    searching: 'Searching',
    sortingBlurb: 'Put scattered elements in order. Compare, swap, shift — you see all of it.',
    searchingBlurb: 'Hunt for one value in an array. Watch a single move wipe out half the list.',
    sortingPick: 'Pick one. Type your array. Then keep pressing Next.',
    searchingPick: 'Enter a target and watch the algorithm hunt it down.',
    nAlgos: (n: number) => `${n} algorithms →`,
    foot: 'Numbers, letters or whole words — all three work. Max 14 elements.',
    home: '← home',
    average: 'average',
    space: 'space',
  },

  ui: {
    backAll: '← all algorithms',
    array: 'Array',
    target: 'Looking for',
    visualise: 'Visualise it',
    randomNums: '🎲 Numbers',
    randomLetters: '🔤 Letters',
    randomWords: '💬 Words',
    prev: '◀ Back',
    play: '▶ Auto',
    pause: '❚❚ Pause',
    next: 'Next ▶',
    speed: 'Speed',
    kbdHint: 'Keyboard: ← → to step, Space to auto-play, P to poke Hootie',
    petNote: 'Hootie’s note',
    whenUse: 'When to use it —',
    code: 'Code',
    counters: 'Counters',
    comparisons: 'Comparisons',
    swaps: 'Swaps',
    writes: 'Writes',
    legend: 'What the colours mean',
    legCmp: 'Being compared',
    legSwp: 'Swap / write',
    legPiv: 'Pivot or minimum',
    legLok: 'Locked in final place',
    tempBuffer: 'temp buffer',
    keyLabel: 'key',
    themeToLight: 'Switch to light mode',
    themeToDark: 'Switch to dark mode',
    language: 'Language',
    sfxOn: 'Sound effects: on',
    sfxOff: 'Sound effects: off',
    musicOn: 'Music: on',
    musicOff: 'Music: off',
    pokeHint: 'Poke Hootie',
  },

  err: {
    empty: 'The array is empty. Enter numbers, letters or words, separated by commas.',
    tooMany: (max: number) => `Maximum ${max} elements. Any more and nothing stays readable on screen.`,
    targetOne: 'Put exactly one value in the target box.',
    typeMismatch: 'Target and array must be the same type — either both numbers, or both text.',
  },

  tag: {
    start: 'Start',
    done: 'Done',
    pass: (n: number) => `Pass ${n}`,
    round: (n: number) => `Round ${n}`,
    insert: (n: number) => `Insert ${n}`,
    level: (n: number) => `Level ${n}`,
    range: (lo: number, hi: number) => `Range ${lo}–${hi}`,
    buildHeap: 'Build Heap',
    extract: (n: number) => `Extract ${n}`,
    scan: 'Scan',
    found: 'Found',
    notFound: 'Not found',
    setup: 'Setup',
    search: 'Search',
  },

  algo: {
    bubble: {
      name: 'Bubble Sort',
      tagline: 'The big element slowly bubbles its way to the end',
      idea: 'Compare adjacent elements and swap them when they are out of order. After every pass, one more element has reached its final home.',
      when: 'Never reach for this in a real project. It exists to teach — nothing explains the idea of a swap better.',
      code: [
        'for i = 0 … n-2:                 // one pass',
        '  for j = 0 … n-i-2:             // the unsorted part',
        '    compare a[j] and a[j+1]',
        '    if a[j] > a[j+1]:',
        '      swap(a[j], a[j+1])',
        '  // largest element is now fixed at the end',
        '  if no swap happened this pass → break',
      ],
    },
    selection: {
      name: 'Selection Sort',
      tagline: 'Find the smallest, bring it forward, lock it down',
      idea: 'Find the minimum of the remaining part and swap it to the front. Exactly one swap per round.',
      when: 'When writing is expensive (flash memory, for example) — it performs at most n-1 swaps.',
      code: [
        'for i = 0 … n-2:',
        '  min = i',
        '  for j = i+1 … n-1:',
        '    if a[j] < a[min]: min = j',
        '  swap(a[i], a[min])',
        '  // a[i] is now in its final position',
      ],
    },
    insertion: {
      name: 'Insertion Sort',
      tagline: 'Exactly how you sort cards in your hand',
      idea: 'Keep the left side always sorted. Pick up the next element and slide it into its correct slot, shifting the bigger ones right.',
      when: 'The fastest thing going for small or nearly-sorted arrays. That is why Timsort runs it on small chunks internally.',
      code: [
        'for i = 1 … n-1:',
        '  key = a[i];  j = i-1',
        '  while j >= 0 and a[j] > key:',
        '    a[j+1] = a[j];  j--',
        '  a[j+1] = key',
        '  // a[0..i] is sorted now',
      ],
    },
    merge: {
      name: 'Merge Sort',
      tagline: 'Split it, sort it, stitch it back',
      idea: 'Keep halving the array. A single element is sorted by definition. Then merge two sorted pieces into one sorted run.',
      when: 'When you need the worst case to stay fast, or you are sorting a linked list. You pay for it in extra memory.',
      code: [
        'mergeSort(lo, hi):',
        '  if lo >= hi: return          // single element',
        '  mid = (lo + hi) / 2          // split down the middle',
        '  mergeSort(lo, mid);  mergeSort(mid+1, hi)',
        '  merge: smaller of the two fronts goes first',
        '  whatever is left over is appended',
        '  copy the temp buffer back into the array',
      ],
    },
    quick: {
      name: 'Quick Sort',
      tagline: 'Pick a pivot, split the world in two',
      idea: 'Choose a pivot. Everything smaller goes left, everything bigger goes right. The pivot lands on its final spot, then you repeat on both halves.',
      when: 'The fastest in-place sort in practice. The worst case only shows up when the pivot is always an extreme value.',
      code: [
        'quickSort(lo, hi):',
        '  pivot = a[hi]                // last element',
        '  i = lo - 1                   // boundary of the smaller part',
        '  for j = lo … hi-1:',
        '    if a[j] <= pivot: i++; swap(a[i], a[j])',
        '  swap(a[i+1], a[hi])          // pivot to its final spot',
        '  quickSort(lo, i);  quickSort(i+2, hi)',
      ],
    },
    heap: {
      name: 'Heap Sort',
      tagline: 'Float the biggest to the top, then throw it to the back',
      idea: 'Turn the array into a Max Heap — every parent bigger than its children. The root is always the largest, so send it to the end and shrink the heap.',
      when: 'When you want a guaranteed O(n log n) and no extra memory. It is not cache-friendly, so it feels slower than Quick Sort.',
      code: [
        'buildMaxHeap: i = n/2-1 … 0 → heapify(i, n)',
        'heapify(i, size):  children = 2i+1, 2i+2',
        '  largest = max(a[i], a[2i+1], a[2i+2])',
        '  if largest != i: swap → heapify(largest)',
        '',
        'for end = n-1 … 1:',
        '  swap(a[0], a[end]);  heapify(0, end)',
      ],
    },
    linear: {
      name: 'Linear Search',
      tagline: 'Check them one by one, stop when you hit it',
      idea: 'Start at index 0 and compare every element to the target. Return the index on a hit, -1 otherwise.',
      when: 'Unsorted data, a small array, or a one-off lookup — you save the cost of sorting first.',
      code: [
        'for i = 0 … n-1:',
        '  if a[i] == target: return i',
        'return -1                        // not found',
      ],
    },
    binary: {
      name: 'Binary Search',
      tagline: 'Throw away half the array at every step',
      idea: 'Look at the middle of a sorted array. If the target is smaller, drop the right half; if bigger, drop the left. Every comparison halves the search space.',
      when: 'Always, on sorted data. Even a million elements only take about 20 comparisons.',
      code: [
        '// precondition: the array must be sorted',
        'low = 0;  high = n-1',
        'while low <= high:',
        '  mid = (low + high) / 2',
        '  if a[mid] == target: return mid',
        '  if a[mid] < target: low = mid + 1',
        '  else: high = mid - 1',
      ],
    },
  },

  /* ── narration. Every key here is referenced by engine.js as { k, p }. ── */
  say: {
    bubble: {
      intro: 'Bubble Sort is as simple as it gets: compare the two neighbours, swap if they are the wrong way round. After each pass the biggest element has reached the end. 🫧',
      passStart: (n: number, last: number, locked: number) =>
        `Pass ${n} begins. We only look at index 0 through ${last} — the last ${locked} are already locked.`,
      compare: (a: V, b: V) => `${a} and ${b} — compare the two.`,
      swapNeeded: (a: V, b: V) => `${a} > ${b}, wrong order. They have to swap.`,
      swapDone: 'Swapped. The bigger one has bubbled a step further along.',
      inOrder: (a: V, b: V) => `${a} ≤ ${b} — already in order. No swap needed.`,
      passEnd: (n: number, v: V) => `Pass ${n} is done. ${v} is locked into its final place. ✅`,
      noSwaps: 'Not a single swap in the whole pass — which means the array was already sorted. That is Bubble Sort’s O(n) best case.',
      done: (c: number, s: number) => `Sorted. That took ${c} comparisons and ${s} swaps. 🎉`,
    },
    selection: {
      intro: 'Selection Sort: each round, find the smallest element in what is left and bring it to the front. Only one swap per round.',
      roundStart: (n: number, i: number, v: V) =>
        `Round ${n}. For now assume index ${i} holding ${v} is the smallest. Now we check everything after it.`,
      compare: (a: V, min: V) => `${a} against the current minimum ${min}.`,
      newMin: (v: V, j: number) => `${v} is smaller. The new minimum is index ${j}.`,
      swap: (min: V, i: number, v: V) => `The round’s minimum is ${min}. Swap it with ${v} at index ${i}.`,
      alreadyMin: (v: V) => `${v} was already the smallest — swap saved. 😎`,
      lock: (v: V) => `${v} is locked into its final position. ✅`,
      done: (c: number, s: number) =>
        `Sorted! Notice — ${c} comparisons but only ${s} swaps. That is why Selection Sort earns its keep when writes are expensive.`,
    },
    insertion: {
      intro: 'Insertion Sort is just sorting cards in your hand. Treat the left side as sorted, and slide each new element into its right slot.',
      pick: (key: V, left: string) => `Now pick up ${key}. The left part [${left}] is already sorted.`,
      lift: (key: V, i: number) => `${key} is lifted out — index ${i} is now an empty hole. We shuffle that hole backwards.`,
      shift: (a: V, key: V) => `${a} > ${key} — so ${key} belongs before it. Shift ${a} one step right.`,
      shifted: (j: number) => `Shifted. The hole has moved to index ${j}.`,
      stop: (a: V, key: V) => `${a} ≤ ${key} — stop right here. The hole is exactly where ${key} belongs.`,
      leftEdge: (key: V) => `We hit the left edge of the array — ${key} is the smallest so far.`,
      place: (key: V, at: number) => `${key} goes into index ${at}. The left side is sorted again. ✅`,
      done: 'Sorted! On an almost-sorted array this hits O(n) — which is why it wins on small or nearly-ordered data.',
    },
    merge: {
      intro: 'Merge Sort is divide and conquer: keep splitting the array in half until single elements are left, then stitch the sorted pieces back together.',
      single: (v: V) => `[${v}] is a single element — and a single element is always sorted.`,
      split: (lo: number, hi: number, mid: number) => `Split range ${lo}–${hi} down the middle: ${lo}–${mid} and ${mid + 1}–${hi}.`,
      mergeStart: (L: string, R: string) => `Now merge the two sorted pieces: [${L}] and [${R}].`,
      compare: (l: V, r: V) => `${l} vs ${r} — which is smaller? That one goes into the buffer first.`,
      take: (v: V) => `${v} is smaller → into the buffer.`,
      drain: (out: string) => `One side is empty — the rest just gets appended. Buffer ready: [${out}].`,
      copy: (v: V, at: number) => `Buffer → index ${at} gets ${v}. (The faded boxes ahead are still old values; they get overwritten too.)`,
      rangeDone: (lo: number, hi: number) => `Range ${lo}–${hi} is sorted now. ✅`,
      done: 'Sorted! O(n) work per level across log n levels — hence O(n log n) in every case. The cost is O(n) extra space.',
    },
    quick: {
      intro: 'Quick Sort: choose a pivot, push everything smaller to its left and everything bigger to its right. The pivot lands on its final spot, then repeat on both sides.',
      pivotPick: (lo: number, hi: number, pivot: V) => `Range ${lo}–${hi}. Let us make the last element, ${pivot}, the pivot.`,
      compare: (v: V, pivot: V) => `Compare ${v} against the pivot ${pivot}.`,
      swapLeft: (v: V, pivot: V, other: V) => `${v} ≤ ${pivot}, so it belongs on the left. Swap it with ${other}.`,
      alreadyLeft: (v: V, pivot: V) => `${v} ≤ ${pivot}, and it is already in the right place. Boundary moves up one.`,
      stayRight: (v: V, pivot: V) => `${v} > ${pivot} — leave it on the right side.`,
      placePivot: (pivot: V, at: number) => `Now bring the pivot ${pivot} to the boundary — swap with index ${at}.`,
      pivotLocked: (pivot: V, at: number) => `Pivot ${pivot} is locked at its final position ${at}. Smaller on the left, bigger on the right. ✅`,
      single: (v: V) => `${v} is the only one left — it is already where it belongs. ✅`,
      done: 'Sorted! O(n log n) on average, but if the pivot is always the largest or smallest you get O(n²). That is why real code picks a random or median pivot.',
    },
    heap: {
      intro: 'Heap Sort: first turn the array into a Max Heap (every parent bigger than its children). Then keep moving the root to the back — the root is always the largest.',
      children: (i: number, v: V, kids: string) => `Parent index ${i} (${v}) has children at index ${kids}.`,
      leaf: (i: number) => `Index ${i} has no children — it is a leaf node.`,
      cmpLeft: (child: V, largest: V) => `Left child ${child} vs ${largest}.`,
      cmpRight: (child: V, largest: V) => `Right child ${child} vs ${largest}.`,
      swapChild: (child: V, parent: V) => `Child ${child} is bigger than parent ${parent} — swap, or the heap rule breaks.`,
      reheapify: 'After the swap the subtree below might be broken — heapify again down there.',
      parentOk: (v: V) => `Parent ${v} is already the biggest. Heap rule intact.`,
      built: (root: V) => `The Max Heap is built. The root ${root} is now the largest element in the whole array.`,
      extract: (root: V, end: number) => `Root ${root} is the largest — send it to index ${end}.`,
      locked: (v: V, size: number) => `${v} is locked ✅. The heap is down to ${size} now, and the root is wrong — heapify again.`,
      done: 'Sorted! A guaranteed O(n log n) with O(1) space — none of the extra memory Merge Sort needs.',
    },
    linear: {
      intro: (target: V) => `Linear Search: walk from the start, one at a time, until ${target} turns up. Sorted or not makes no difference.`,
      check: (i: number, v: V, target: V) => `Index ${i} holds ${v}. Is that ${target}?`,
      found: (target: V, i: number, c: number) => `Yes! ${target} found at index ${i}. Took ${c} comparisons. 🎯`,
      next: 'Nope. Move along.',
      notFound: (target: V) => `Checked the whole array, ${target} is nowhere in it. Return -1. That is the O(n) worst case.`,
    },
    binary: {
      introSorted: 'Binary Search has one precondition: the array must be sorted. Yours is, so off we go.',
      introUnsorted: 'Hold on! Binary Search only works on a sorted array. I have sorted it for you first — now watch.',
      range: (low: number, high: number) => `The current search area runs from index ${low} to ${high}.`,
      mid: (low: number, high: number, mid: number, v: V) => `Mid = (${low}+${high})/2 = ${mid}. That holds ${v}.`,
      found: (target: V, mid: number, c: number) => `Got it! ${target} is at index ${mid}. Only ${c} comparisons — 🎯`,
      goRight: (v: V, target: V, low: number) => `${v} < ${target}, so the target must be on the right. Blow away the left half. low = ${low}.`,
      goLeft: (v: V, target: V, high: number) => `${v} > ${target}, so the target must be on the left. Blow away the right half. high = ${high}.`,
      notFound: (target: V) => `low has run past high — the search area is gone. ${target} simply is not in the array. Return -1.`,
    },
  },

  /* Random one-liners when the user pokes the owl. */
  petTips: [
    'Hoot! Try the same array on two different algorithms and watch the counters.',
    'Psst — Binary Search only earns its speed on a sorted array.',
    'Big-O describes how things grow, not how fast they are on 8 elements.',
    'Stable sort = two equal values keep their original order. Handy when sorting by two keys.',
    'Try typing words instead of numbers. Sorting is just "compare and move" either way.',
    'Insertion Sort on already-sorted data is O(n). Give it a try!',
    'Quick Sort is usually the fastest here — until the pivot goes badly wrong.',
  ],
};

export type Dict = typeof en;
