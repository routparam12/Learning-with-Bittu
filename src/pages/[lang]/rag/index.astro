---
import Page from '../../../sections/rag/components/Page.astro';

export function getStaticPaths() {
  return [{ params: { lang: 'en' } }, { params: { lang: 'hi' } }];
}

const { lang } = Astro.params;
---
<Page locale={lang} />
